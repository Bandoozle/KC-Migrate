import type { ReactNode } from "react";

/** Pass-through so nested HVAC Lead Generation can stay on the WordPress shell. */
export default function HvacMarketingLayout({ children }: { children: ReactNode }) {
  return children;
}
